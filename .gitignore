# Claude local settings (machine-specific paths)
.claude/settings.local.json

# MCP server config (machine-specific absolute paths to local bridge)
.mcp.json

# Build artifacts (ESP-IDF)
build/
managed_components/
# dependencies.lock — tracked (pins working component versions)
sdkconfig
sdkconfig.old
*.log

# OS / editor
Thumbs.db
.DS_Store
.vscode/

# Reference material — large/redistribution-iffy datasheets, diagrams, photos.
# Keep the folder's README tracked; ignore the dropped binaries.
hardware/reference/*
!hardware/reference/README.md

# Waveshare factory binary (large, redistribution-iffy) — keep local
tools/waveshare-factory/*.bin
